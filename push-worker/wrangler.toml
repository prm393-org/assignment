name = "chuoi-push"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Secrets are NOT set here (they would be committed). Set them after deploy:
#   npx wrangler secret put PUSH_API_KEY
#   npx wrangler secret put FIREBASE_SERVICE_ACCOUNT
# See README.md.
